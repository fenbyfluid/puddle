[package]
name = "linmot"
version = "0.1.0"
edition = "2024"

[features]
default = []
serde = ["dep:serde_core"]

[dependencies]
bitflags = { version = "2.10.0" }
serde_core = { version = "1.0.228", optional = true }

[dev-dependencies]
serde_test = "1.0.176"
